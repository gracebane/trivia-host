{
  "name": "trivia",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --persist-to /tmp/trivia-wrangler-state",
    "start": "wrangler dev --persist-to /tmp/trivia-wrangler-state",
    "test": "node --test test/",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
